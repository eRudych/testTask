export const chartInitialState = {
  isLoading: false,
  data: {},
  options: {}
};

export default (state = chartInitialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
